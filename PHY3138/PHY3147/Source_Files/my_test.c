#include <stdio.h>

int main() {

    int my_list[101];
    int angle = 50;
    for(int i = 0; i < 101; i++) {
        my_list[i] = i;
    printf("angle%d RH%d", angle, my_list[i]);
    printf("\n");
    }

    return 0;
}
